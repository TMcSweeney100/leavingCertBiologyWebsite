# Handoff — coursework pilot

Rewritten at the end of every session: where things stand, what's half-done, what's waiting on a human. Anything permanent goes elsewhere (`docs/ARCHITECTURE.md` for how the code and hosts work, `docs/PILOT-ROADMAP.md` §2–§3 for decisions, the plans for per-task deviations). The live BiPi site's final handoff is archived at `docs/archive/bipi-site/HANDOFF.md`; its open items (Katelyn's copy sign-offs, print page count) still stand.

**Standing rule (16 Sep 2026):** `docs/ARCHITECTURE.md` describes the code as built. When a session changes anything it describes (a filter, a pattern, an env var, a file's job), it fixes that section in the same commit, and at the end of the session checks it for anything else the session made false. A wrong map is worse than none.

## Where things are (18 Sep 2026: 2A–2F built — Phase 2 is done; Gate P2 is code-and-tests green, human review still open)

- **2F Personal items and timeline is built** (`pilot/2f-personal-items-and-timeline`, branched from `pilotMain` after 2E): `V10__personal_items.sql` adds `personal_item` (owner `student_user_id`, `title`, `due_date`, `kind`, an optional `class_group_id` label). `PersonalItemRepository` (`ie.coursework.timeline.adapter.persistence`) has every method take the owner and filter by it in the `WHERE` — deliberately no lookup by id alone — and no other feature joins the table. `TimelineRepository.between` is one union query merging approved-enrolment stage dates, approved-enrolment dated active teacher items, and the student's own personal items into one list ordered by date, then kind, then subject and ordinal; `TimelineRange.problem(from, to)` (`timeline/domain/`) caps any range at 100 days and rejects `to` before `from`. `GET /me/timeline?from=&to=` and `GET/POST/PATCH/DELETE /me/personal-items[/{id}]` are built; `TimelineService.add`/`edit` label an item with a class only when the student has actually joined it. `PersonalItemPrivacyTest` (`timeline/authz/`) proves a teacher, a school leader and another student all get 404 from every personal-item endpoint for an id that isn't theirs, with a mutation-and-revert bite test that a naive "any row with this id" query would slip past. **A follow-up fix in this task's area (commit `7244574`):** `TimelineService.edit()` was found in review to re-validate a personal item's class label (`checkClass`) on every edit, even when the class hadn't changed — so a student removed from a class could no longer edit any field of an item they'd labelled with it. Fixed so `checkClass` only re-runs when the submitted `classId` actually differs from the item's stored one. The frontend adds Zod schemas mirroring `TimelineViews`, `lib/app/timeline.ts`'s pure calendar arithmetic (`rangeFor`, `stepFrom`, `relativeDay`, `rangeLabel`, `weekday`, `monthWeeks`, all UTC-midnight date-string math, no timezone/DST risk), `TimelineView` (list/week/month, previous/next, item kind always in words), `PersonalItemForm`/`AddPersonalItem`/`PersonalItemActions` (add/edit/delete with in-place delete confirmation), and `/home` is now the student's timeline page with empty-state notices for no classes, pending-only classes, and no components yet. `make verify` is green: 348 backend tests (329 after 2E); 110 `node:test`, 129 Vitest specs. `e2e/phase2.e2e.ts` is extended with the merged-timeline assertion (stage date, teacher item and personal item, in order, all labelled "Biology"); `make e2e` was started separately in this session on a background process to confirm it passes on laptop and phone with the new timeline test — its result isn't independently confirmed in this note, so treat it as pending until the next session checks it. **Task 7 (restyle from D-4) is skipped**: `docs/design/pilot/D-4-timeline/` doesn't exist yet, the same pattern as 2D's D-5 and 2E's D-3 skips; the restyle is still open (see "Waiting on a human").
- **2E Student component page is built** (`pilot/2e-student-component-page`, branched from `pilotMain` after 2D): `V9__item_ticks.sql` adds `item_tick` (primary key `student_user_id, teacher_item_id`; unticking clears `done_at` rather than deleting the row, keyed on student and component per review issue #3, not enrolment). `CheckpointState.at(stageDate, today)` (`components/domain/`) is `DUE` the day after a stage's date, `NOT_DUE` before that or with no date; `DublinDate.today(clock)` reads the one `Clock` bean in `Europe/Dublin`, not server UTC. `ComponentRepository.findForApprovedStudent` and `forStudent` scope a component to an approved student the same way `owned` scopes it to a teacher. `ComponentService.studentView` builds the full page — stages with dates, checkpoint states, active teacher items with the student's own ticks, prompts, report sections, mark bands, brief rules and `today` — and `ComponentService.view` now dispatches on role: a teacher's `owned` component gets the setup view, anyone else falls through to `findForApprovedStudent` or 404. `PUT /components/{id}/teacher-items/{itemId}/tick` lets only the approved student tick their own copy (idempotent; the teacher gets 404); `GET /me/components` lists a student's own components for navigation. `ComponentScopeTest` was extended to prove a pending, removed or outside student can't read the view or tick, and that a removed student specifically can't tick (a case added after the first pass only proved pending/outside). The frontend adds `lib/app/component-progress.ts`'s pure `progress()` (current stage, per-stage state, phase, countdown, derived from the server's `today`, never the browser's clock) and the page itself: `ComponentOverview`, `ItemTick` (a checkbox whose accessible name is the item's text, kept separate from the due date shown beside it — a follow-up fix after the first pass conflated them), `ComponentTabs`, `MyComponents` on `/home`, and `app/(app)/components/[id]/page.tsx` (a teacher who opens it is redirected to their class's Component tab, P2-41). `make verify` is green: 329 backend tests (311 after 2D); 101 `node:test`, 119 Vitest specs. `e2e/phase2.e2e.ts` is extended with the student half of the journey (open the component page, see the current stage and countdown, tick and untick a teacher item), run on `laptop` and `phone` with axe. **Task 8 (restyle from D-3) is skipped**: `docs/design/pilot/D-3-student-component/` doesn't exist yet, so the page stays working-first (roadmap §6.3), same as 2D's D-5 skip; the restyle is still open (see "Waiting on a human").
- **2D Teacher component setup is built** (`pilot/2d-teacher-component-setup`, branched from `pilotMain` after 2C): `V8__component_instances.sql` adds `component_instance` (one per class, pins a brief), `instance_stage_date` (a stage's due date) and `teacher_item` (a teacher's own to-dos inside a stage, retired by timestamp, never deleted), plus a trigger that refuses any stage date or teacher-item due date after the brief's completion date. `ComponentService`/`ComponentController` (`ie.coursework.components`) list published briefs for a class's subject, create a component, return the teacher's role-shaped view, save stage dates as a batch (refusing any after the completion date, by stage name, and warning on out-of-order dates or a brief whose completion date the SEC has since moved), and add/edit/retire the teacher's own items within a stage. `ComponentScopeTest` proves every component and teacher-item endpoint 404s for anyone but the owning teacher — including another teacher's item reached through their own component — and was checked to actually catch a broken scope check, not pass vacuously. The frontend adds a Component tab (`/teach/classes/[id]/component`) with `CreateComponentForm` (brief chooser), `StageDatesForm` (batch save) and `TeacherItems` (add/edit/retire, confirmed in place like D-2's Remove); the Students and Component tabs now share `ClassHeader`. `make verify` is green: 311 backend tests (262 after 2C); 96 `node:test`, 106 Vitest specs. `make e2e` is green on `laptop` and `phone` (`phase2.e2e.ts`: a teacher creates a component, is refused a date after the completion date by name, fixes it, and adds a teacher item), axe clean throughout. **Task 10 (restyle from D-5) is skipped**: `docs/design/pilot/D-5-teacher-component-setup/` doesn't exist yet, so the Component tab stays working-first (roadmap §6.3); the restyle is still open (see "Waiting on a human").
- **2C Business content and 2027 briefs is built** (`pilot/2c-business-and-briefs`, branched from `pilotMain` after 2B): `db/content/V3__business_template.sql` loads the Business template (six numbered stages plus the unnumbered Compilation, five sections with suggested word counts, four named mark bands, the guidelines' prompts including Appendices One and Two) and publishes it. `db/content/V4__briefs_2027.sql` loads one `annual_brief` per template — the four **final** 2027 briefs, never the `EN-EX` samples — each pinning version 1, with its topic, completion date, limits and formatting-rules table as `brief_rule` rows. `SourceTextTest` (2B) is extended to the brief's topic, words-not-counted, image note and formatting rules; the extension was checked to actually catch a substituted rule value before being trusted. `make verify` is green: 262 backend tests (242 after 2B, plus `BusinessTemplateContentTest`, `BriefContentTest` and two more `ContentMigrationsTest` cases); `SourceTextTest` now checks 434 strings. **Tim's content review of V3 and V4 against the PDFs is still open** (Gate P2 item, see "Waiting on a human" below).
- **2B Science content is built** (`pilot/2b-science-content`, branched from `pilotMain` after 2A): `db/content/V2__science_templates.sql` loads the Biology, Chemistry and Physics templates (one content source, wording quoted per subject per P2-9) and publishes them. A test-only `content.SourceDocuments`/`SourceRef` helper (PDFBox) reads the SEC and NCCA PDFs in `docs/newDevelopement/subjectDocs/`, and `SourceTextTest` checks every quoted string (over 150) against the page its `source_ref` names — verified to actually catch a substituted quote, not just pass vacuously. The four 2027 SEC briefs were renamed to their SEC codes in the same branch (`SEC-2027L0*-*-brief.pdf`), and the duplicate Biology copy under `docs/` was removed. `make verify` is green: 242 backend tests (201 after 2A, plus `SourceDocumentsTest` and `SourceTextTest`, and `ScienceTemplateContentTest`'s 32 parameterized cases). **Tim's content review of V2 against the PDFs is still open** (Gate P2 item, see "Waiting on a human" below); so is Katelyn's review of the Biology checkpoints.
- **2A Template schema is built** (`pilot/2a-template-schema`, branched from `pilot/design-prompts-d3-d5`): `V6__templates_and_briefs.sql` (`component_template` → `template_version` → stages, sections, mark bands, checkpoints, prompts, and `annual_brief`/`brief_rule`) and `V7__template_guards.sql` (triggers that freeze a PUBLISHED or RETIRED version's structure while still taking text corrections, and the matching brief guard). `make verify` is green: 201 backend tests (146 before 2A, plus four new classes: `TemplateSchemaTest`, `ContentResetPolicyTest`, `TemplateStructureGuardTest`, `BriefGuardTest`). No app-facing UI in this milestone — it's schema and triggers only, so nothing needed a design placeholder.
- **All six Phase 2 plans are written** (2A–2F, in `docs/superpowers/plans/2026-09-16-pilot-2*.md`, listed at the top of the roadmap), and **all six milestones are now built.** Phase 2's code and tests are done; Gate P2 itself isn't fully walked yet because the human-review items below are still open. Continue with Phase 3 (3A Log model) once Gate P2's human items are resolved, or sooner if Tim wants the build to keep moving ahead of the reviews.
- **The content and SQL in the plans were checked before they were written down.** On a throwaway Postgres 18 (not the dev database): migrations V1–V5 as built, then the planned V6–V10, the three content migrations and the timeline query. Every one of the 434 quoted strings in 2B/2C was found on the PDF page its `source_ref` names. The triggers refused what they should and allowed text corrections.
- **Findings that changed the plan from the design.** The three science guidelines aren't word-for-word identical: punctuation differs, Chemistry and Physics say "school laboratory", and their Stage 4 authentication sentence differs. So quotes are per subject and only structure is shared (P2-9). The Physics guideline heads Stage 1 "Initial Response to the Brief"; the plans use the briefs' stage names (P2-10). Neither the SEC nor the NCCA says which stage each report section is written during (Q-P2-B). The BiPi components need teacher-written fields the template doesn't have, so 2E builds app components (Q-P2-C).
- **Plan decisions P2-1 to P2-52 are proposed, not confirmed**, except three Tim confirmed on 16 Sep 2026: no section-to-stage mapping (Q-P2-B, P2-13), new app components for the student page (Q-P2-C, P2-33, and §8.2's 2E row reworded), one component per class (Q-P2-D, P2-23). Three questions are still open: roadmap §3 "Phase 2 questions". The plans are written to a stated default for each, so building doesn't wait on them.

- **Phase 1 is built, restyled and merged.** 1A–1D are all in `pilotMain`: PRs #3, #4 and #5. The D-1/D-2 restyle (`pilot/1d-restyle`) was merged at `78ef007` on 16 Sep 2026 without a PR page: this machine has no `gh`, so it was merged locally at Tim's request.
- **1D Task 11 is done.** Design packs D-1 and D-2 are in `docs/design/pilot/`. Every app page now uses the pack designs. `make verify` is green (155 backend tests; 92 `node:test`, 90 Vitest specs, lint, types, build), and `make e2e` is green on `laptop` and `phone`, with axe clean on every page in the journey, including the inline reset-code panel.
- **The app no longer uses the BiPi look (roadmap R25).** Tim chose direction 2e "Navy": navy `#1F3A6E` accent, amber `#A04806` only for pending work, cool graphite neutrals, 10/8/6px radii. Only the typefaces are still shared with BiPi. `UI-BRIEF.md` §5 is rewritten; `--app-*` tokens apply through `.app-theme`; `--bipi-*` and the live schedule are untouched.
- **Decisions Tim confirmed this session:** R26, a school short name for the header (`school.short_name`, migration `V5`, `create-school --short-name`, `set-school-short-name`, `Me.roles[].schoolShortName`); R27, class-page additions (Turn joining off and Remove confirm in place, copy join code with "Copied", "asked … ago", the reset code inline in its row with Hide code and a can't-be-undone warning); R28, "Leaving Cert Practical" and the NWETSS crest as placeholders until H4. Each pack's `NOTES.md` ends with the answers to its open questions.
- **`make e2e` hardening:** `scripts/e2e.sh` refuses to start if :8081 or :3100 is taken, and kills whatever listens there at teardown. An orphaned `next-server` from an earlier run had been answering the readiness check. The axe helper waits for running animations to settle.

## Not done, and worth a follow-up

- **Two `UI-CHECKLIST.md` items were already missing and are still open.** Submit buttons don't change to a present-participle label while busy ("Signing in…"; they only disable), and a failed submit doesn't move focus to the alert. Both need a spec each.
- **Sign in stays enabled during a lockout.** D-1 draws it disabled, but the API returns the wait only as text in `detail` (roadmap §6.2 `/login` row). Disabling it would need a machine-readable retry time from `LoginThrottle`.
- **On phones, student rows wrap their buttons under the name.** "Issue reset code" plus "Remove" is wider than the space D-2 leaves beside a name at 390px. Pending rows fit as drawn.
- **`ComponentService.owned` checks the component's own owner filter and `ClassService.owned` independently (defense in depth, found reviewing 2D Task 7).** `ComponentScopeTest` proves the endpoints 404 correctly today, but a future regression that broke only `ComponentRepository.findOwned`'s owner filter wouldn't be caught by any current test unless `ClassService.owned` broke at the same time. Worth a test that isolates each check; not a defect and not blocking.
- **`TeacherItems`' `editing` and `retiring` local state are independent (found reviewing 2D Task 9).** A teacher editing one item's text or date who then retires a *different* item in the same stage has their in-progress edit silently discarded — no data corruption, just a lost unsaved draft, with no confirmation shown. Low risk, narrow edge case, worth a fast-follow fix.

## Gate P1 — walked locally, 16 Sep 2026

- [x] `make verify` green — 151 backend tests; 85 `node:test`, 75 Vitest specs, lint, types, build.
- [x] `make e2e` green on `laptop` and `phone`: the whole roadmap §8.1 journey plus a keyboard-only sign-in; axe clean on every page visited.
- [ ] The same journey by hand on the Vercel Preview against Render (operator commands from Render's Shell; see `ARCHITECTURE.md` §9.1 for the Starter-instance trick).
- [ ] Keyboard-only run of the whole journey by hand on a laptop.
- [ ] Tim has reviewed the pages (now restyled; the D-1/D-2 frames are in `docs/design/pilot/`).

Gates 1A and 1B passed on the real host on 16 Sep 2026; Gate 1C passed locally the same day (all nine checkpoints, not yet walked on the preview). The records are in the roadmap's status board; what's still open from them is listed below.

## Gate P2 — code and tests green, 18 Sep 2026; human review still open

- [x] `make verify` green across 2A–2F: 348 backend tests; 110 `node:test`, 129 Vitest specs, lint, types, build.
- [ ] `make e2e` on `pilot/2f-personal-items-and-timeline` with the extended journey (teacher creates a Biology component, sets dates, a date after the completion date is refused by name; student sees the stages, adds a personal item; the timeline shows both, in order, labelled). A run was started separately from this session in the background; its pass/fail wasn't independently confirmed while this note was written — check it before treating this line as done.
- [x] Changing a published template's structure in a migration fails the test suite (2A: `TemplateStructureGuardTest`, `BriefGuardTest`).
- [ ] **Tim's content review is still open.** V2 (Biology, Chemistry, Physics, 2B) and V3/V4 (Business and the four 2027 briefs, 2C) are built but not yet checked line-by-line against the PDFs. Checklists are in plan 2B Task 5 Step 3 and plan 2C Task 4 Step 2 (see "Waiting on a human" below). This was flagged open after 2B and 2C and hasn't happened since — nothing in this session's work touched content, so it's unchanged.
- [ ] **Katelyn's review of the Biology checkpoints is still open**, same as above.
- [ ] Design packs D-3 (student component), D-4 (timeline) and D-5 (teacher component setup) haven't arrived; 2D, 2E and 2F were all built working-first without them (their restyle tasks are each individually skipped, noted in their own bullets above).
- [ ] PR `pilot/2f-personal-items-and-timeline` → `pilotMain` — not opened; Tim opens and merges PRs (root `CLAUDE.md`).

**What this means:** everything this session (or any 2A–2F session) could build and test is done and green. Gate P2 as a whole isn't walked yet — it's waiting on the same two human reviews it's been waiting on since 2B/2C, plus e2e confirmation and the PR.

## Waiting on a human

- **After deploying 1D to Render, set the short name on any school that already exists:** `operator set-school-short-name --roll=… --short-name=…`. Without it the header shows the full name.
- **Walk Gate P1 on the Vercel Preview**, which also covers Gate 1C's journey on the real host.
- **Phase 2 questions still open: Q-P2-A (stage description sentences), Q-P2-E (completion dates on the timeline), Q-P2-F (brief URLs)** (roadmap §3).
- **Confirm or change plan decisions P2-1 to P2-52** (each plan's decisions table).
- **Design packs D-3, D-4, D-5**: prompts written (`docs/design/prompts/`), packs not back. **2D was built working-first without D-5** (Task 10 skipped, roadmap §6.3), **2E was built working-first without D-3** (Task 8 skipped), and **2F was built working-first without D-4** (Task 7 skipped); all three restyles are still open whenever their packs arrive. The pages are built working-first either way, and Phase 2 is now fully built regardless of when the packs land.
- **The Coursework Rules and Procedures** is still not in `subjectDocs/`. Phase 2 doesn't need it; Phase 6 does (Q6).
- **Content reviews for Gate P2:** Tim reads `V2__science_templates.sql` (built) against the PDFs — checklist in plan 2B Task 5 Step 3 (stages/hours/descriptions, sections/bands, checkpoints and quotes, prompts, per subject; plus whether the checkpoint wording is fit to show teachers, Q1) — and `V3__business_template.sql`/`V4__briefs_2027.sql` (now built) — checklist in plan 2C Task 4 Step 2 (Business stages/hours/sections/bands/checkpoints/prompts; each brief's SEC code, completion date, formatting rules, topic, limits; whether the Physics completion date of 11 Dec 2026 is right for a 6th-year pilot, Q9). Katelyn reviews the Biology checkpoints. Q1 names the other three reviewers. Record each review here with date and what was checked once done.
- **Carried over from earlier gates:** redeploy Render while holding a live session cookie and confirm `/auth/me` still answers (passed locally, not yet on Render); confirm Katelyn's `main` project 404s `/login` and `/api/v1/health`; **delete the gate-check data before any real onboarding** (`Gate Check School` ×2, roll `00009Z`, users `gate.teacher*`, `gate.student.c`; `gate.teacher`'s password is `gate-check-password`); upgrade the Render database before the first real account; roadmap §9 R3–R5 are calendar-bound.

## Half-done

Nothing in code.
